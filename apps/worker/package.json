{
  "name": "@campanha360/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test:dispatch-send-processor": "npx --yes tsx --test src/dispatch-send.processor.spec.ts"
  },
  "dependencies": {
    "@campanha360/shared": "*",
    "@prisma/client": "^6.0.0",
    "bullmq": "^5.30.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
